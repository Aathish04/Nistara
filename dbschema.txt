CREATE TABLE main.posts (
    postid text PRIMARY KEY,
    userid text,
    username text,
    profilephoto text,
    classifier int,
    geolocation frozen<tuple<double, double>>,
    isclassified boolean,
    multimediaurl list<text>,
    textualinfo text,
    timestamp timestamp,
    tag text
)

CREATE TABLE main.requests (
    requestid text PRIMARY KEY,
    requesttype text,
    class text,
    geolocation frozen<tuple<double, double>>,
    iscomplete boolean,
    item text,
    quantity int,
    postid text
    userid text,
    username text,
    profilephoto text,
    timestamp timestamp,
    textualinfo text,
    matcherID int,
)


CREATE TABLE main.donation (
    donationid text PRIMARY KEY,
    donatingitem text,
    geolocation frozen<tuple<double, double>>,
    iscomplete boolean,
    postid text,
    quantity int,
    userid text,
    username text,
    profilephoto text,
    timestamp timestamp,
    textualinfo text,
    class text,
    matcherID int,
);



CREATE TABLE IF NOT EXISTS main.MATCHES
(requestID text,
 donationID text,
 matcherID int,
 matchtime timestamp,
 requesterAck boolean,
 donorAck bolean,
 PRIMARY KEY ((requestID,donationID))
);

CREATE TABLE IF NOT EXISTS main.USERS
(userID int,
 userName text,
 phoneNumber text,
 address text,
 geoLocation tuple<double,double>,
 profileImage text,
 maskedAadhar text,
 PRIMARY KEY (userID)
);

yet to add matcher table